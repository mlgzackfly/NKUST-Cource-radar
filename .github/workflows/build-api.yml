name: Build Static API and Deploy to GitHub Pages

on:
  # 當有新的課程資料時觸發
  push:
    paths:
      - 'data/nkust/ag202/**'
    branches:
      - main

  # 手動觸發
  workflow_dispatch:

  # 定期重建（確保 API 最新）
  schedule:
    - cron: '0 4 * * *'  # 每天 04:00 UTC

permissions:
  contents: read
  pages: write
  id-token: write

# 只允許一個部署同時進行
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-api:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Static API
        run: npm run build:api

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public/api'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Generate API Documentation
        run: |
          echo "📊 API Endpoints Available:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 Base URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/api" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Endpoints:**" >> $GITHUB_STEP_SUMMARY
          echo "- \`/courses.json\` - 所有課程列表" >> $GITHUB_STEP_SUMMARY
          echo "- \`/courses/{id}.json\` - 單一課程詳情" >> $GITHUB_STEP_SUMMARY
          echo "- \`/semesters.json\` - 可用學期列表" >> $GITHUB_STEP_SUMMARY
          echo "- \`/metadata.json\` - API 元資料" >> $GITHUB_STEP_SUMMARY
